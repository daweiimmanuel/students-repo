<% include ../partials/header %>
    <div class="ui main container segment">
        <h1 class="ui center aligned header"><%= event.eventName %> - <%= event.event_date_formatted %></h1>
        <% if(event_students.length){ %>
            <h2 class ="ui header">
                Delete the following students before attempting to delete this event.
            </h2>
            <div class="ui cards">
                <%event_students.forEach(function(student) { %>
                    <div class="card">
                        <div class="content">
                            <a href="<%= student.url %>">
                                <div class="header">
                                    <%= student.name%>
                                </div>
                            </a>
                        </div>
                    </div>
                <% })%>
            </div>
        <% } else { %>
            <h2 class="ui header">
                Do you really want to delete this Event?
            </h2>
            <form class="ui form" action="" method="POST">
                <input type="hidden" name="eventid" required="true" value="<%= event._id %>">
                <button class="ui red button" type="submit">Delete</button>
            </form>
        <% } %>
        
    </div>

<% include ../partials/footer %>