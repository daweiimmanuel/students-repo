<% include ../partials/header %>
    <div class="ui main container segment">
        <p>
        <form class="ui form" action="<%= major.url %>" method="GET">
          <div class="field">
              <div class="ui action input">
                  <input type="text" name="search" placeholder="Student search...">
                  <div class="ui button">
                    Search
                  </div>
              </div>
          </div>
        </form>
        </p>
        <h1 class="ui center aligned header">Students who wants to take <%= major.majorName %></h1>
        <div class="ui cards">
            <%major_students.forEach(function(student) { %>
                <div class="card">
                    <div class="content">
                        <a href="<%= student.url %>">
                            <div class="header">
                                <%= student.name%>
                            </div>
                        </a>
                    </div>
                </div>
            <% })%>
        </div>
        <br>
        <div class="ui container">
            <a href="<%= major.url %>/delete">
                <button class="ui red button" type="submit">Delete Major</button>
            </a>
            <a href="<%= major.url %>/update">
                <button class="ui orange button" type="submit">Update Major</button>
            </a>
        </div>
    </div>

<% include ../partials/footer %>