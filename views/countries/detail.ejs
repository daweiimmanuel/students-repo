<% include ../partials/header %>
    <div class="ui main container segment">
        <p>
        <form class="ui form" action="<%= country.url %>" method="GET">
          <div class="field">
              <div class="ui action input">
                  <input type="text" name="search" placeholder="Student search...">
                  <div class="ui button">
                    Search
                  </div>
              </div>
          </div>
        </form>
        </p>
        <h1 class="ui center aligned header">Students who wants to go to <%= country.countryName %></h1>
        <div class="ui cards">
            <%country_students.forEach(function(student) { %>
                <div class="card">
                    <div class="content">
                        <a href="<%= student.url %>">
                            <div class="header">
                                <%= student.name%>
                            </div>
                        </a>
                    </div>
                </div>
            <% })%>
        </div>
        <br>
        <div class="ui container">
            <a href="<%= country.url %>/delete">
                <button class="ui red button" type="submit">Delete Country</button>
            </a>
            <a href="<%= country.url %>/update">
                <button class="ui orange button" type="submit">Update Country</button>
            </a>
        </div>
    </div>

<% include ../partials/footer %>